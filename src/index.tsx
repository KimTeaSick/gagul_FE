import { View } from "react-native";
import styled from "styled-components/native";

const Texxt = styled.Text`
padding: 4px;
color: #fff;
`

const Main = () => (
  <View>
    <Texxt>asd</Texxt>
  </View>
)

export default Main